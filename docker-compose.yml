version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    env_file:
      - ./backend/.env
    volumes:
      - ./cache:/app/cache
      - ./notes:/app/notes

  frontend:
    build: ./frontend
    ports:
      - "5175:5175"
    depends_on:
      - backend
